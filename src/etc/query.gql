query characters($id: ID!) {
    characters(id: $id) {
        id
        name
        status
        species
        type
        gender
        origin
        location
        image
        episode
        created
    }
}
