query GetCharacters($filters: FilterOptions!) {
    characters(filters: $filters) {
        id
        name
        status
        species
        type
        gender
        origin
        location
        image
        episode
        created
    }
}
